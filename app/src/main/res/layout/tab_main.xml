<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_centerHorizontal="true" android:paddingTop="6dp">
    <ImageView
        android:id="@+id/tab_image"
        android:layout_height="30dp"
        android:layout_width="30dp"
        android:layout_centerHorizontal="true"
       ></ImageView>
    <TextView
        android:id="@+id/tab_text"
        android:textColor="@color/selector_tab_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tab_image"
        android:layout_centerHorizontal="true"
        android:paddingBottom="5dp"/>
</RelativeLayout>